#include <bits/stdc++.h>
using namespace std;

int main(){
    int A,B,C,D,E;
    cin >> A >> B >> C >> D >> E;

    map<int, int> temp;
    temp[A] = 0;
    temp[B] = 0;
    temp[C] = 0;
    temp[D] = 0;
    temp[E] = 0;

    cout << temp.size() << endl;
    return 0;
}