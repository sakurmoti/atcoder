#include <bits/stdc++.h>
using namespace std;

int main(){
    int N,S;
    cin >> N >> S;

    vector<int> a(N),b(N);
    for(int i = 0; i < N; i++){
        cin >> a.at(i) >> b.at(i);
    }

    

    return 0;
}