#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define ALL(x) (x).begin(), (x).end()
#define RALL(x) (x).rbegin(), (x).rend()

#include <atcoder/all>
using namespace atcoder;
using mint = modint998244353;

int main(){
    int N,M,K; cin >> N >> M >> K;
    
    // 最大の和は50 * 50なことに注目して
    // dp[i][S] := i番目までの和がSになるような組み合わせ　と定義する
    
    return 0;
}